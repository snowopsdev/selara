{
  "name": "selara-desktop",
  "private": true,
  "version": "0.6.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:mock": "SELARA_MOCK=1 vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "e2e": "playwright test",
    "e2e:update": "playwright test --update-snapshots",
    "prepare-sidecar": "node scripts/prepare-sidecar.mjs",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-shell": "^2"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.28.2",
    "jsdom": "30.1.0",
    "vite": "^8"
  }
}

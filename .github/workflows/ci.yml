name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: fmt / clippy / test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      # apps/selara-desktop (Tauri) and macOS-only shell paths (Accessibility,
      # global hotkey, egui `serve`) need local macOS testing. Linux CI covers
      # the portable workspace crates/CLI; desktop is excluded here.
      - name: cargo fmt
        run: cargo fmt --all -- --check

      # Warnings are errors: the tree is lint-clean and the objc cfg noise on
      # macOS FFI paths is declared via check-cfg in selara-platform.
      - name: cargo clippy
        run: cargo clippy --workspace --all-targets --exclude selara-desktop -- -D warnings

      - name: cargo test
        run: cargo test --workspace --exclude selara-desktop
